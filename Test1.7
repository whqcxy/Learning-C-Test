int main() {
	int i = 0;
	char password[10] = {0};
	for (i = 0; i < 3; i++) {
		printf("请输入密码：");
		scanf("%s", password);//数组名是一个地址，不用加&，来取地址
		//%s后不能加\n，否则无法读取地址
		if (strcmp(password,"123456")==0){//==不能用来比较两个字符串是否相等，应该使用一个库函数-strcmp来比较
			printf("登录成功");
			break;
		}else {
			printf("密码错误，请再次输入");
		}
	}if (i == 3){
	printf("已输入三次密码错误，无法登录");
	}
	return 0;
}
